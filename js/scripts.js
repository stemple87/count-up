var getRemainder = function(input1, counter) {
  var input1;
  var counter;
  var resultsArray = [];
  for (var i = counter; i <= input1; i += counter) {
    return i;
  }
}


// var results = input1 / counter;
// results = Math.floor(results);

// resultsArray.push(i);

//     var resultsArray = [];
//     resultsArray.push(counter + i);
//     return resultsArray;
// }
